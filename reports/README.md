# Reports

`reports/` stores generated outputs, interim summaries, export-ready files, and other stage-based deliverables.

Current restored content includes:

- `bdlm_union_first_pass/`: legacy BDLM interim analysis outputs restored from the April 2, 2026 local backup
- `tb-research/`: prior TB research reporting outputs restored from the same backup

Use this directory for outputs that are useful to keep locally, but avoid treating it as the primary home for source materials or long-lived project files.
