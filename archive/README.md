# Archive

`archive/` stores legacy materials that should remain accessible without living in the active root structure.

Restored on April 2, 2026:

- `旧文档/`: historical drafts, protocol rewrite materials, and older manuscript assets
- `legacy-root-scripts/`: superseded GitHub sync and one-click helper scripts from the old root layout
- `legacy-scripts/`: older mail and reporting helper scripts replaced by the current Python-based scripts
- `_installers/`, `_docx_edit_tmp/`, `bdlm_temp/`: temporary or environment-specific legacy content kept for reference only

Intentionally not restored into the active root:

- `.codex/`
- `.agents/`
- old `.git/` state

Those remain preserved inside the backup zip and git bundle.
